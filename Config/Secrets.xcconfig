// Committed default API base URL (not a secret. the Worker URL is public in the app binary).
// For machine-specific URLs, copy Secrets.xcconfig.example and add this file to .gitignore
// (see the commented line in the root .gitignore).

SLASH = /
CINESCROLL_API_BASE_URL = https:$(SLASH)/cinescroll-api.cinescroll-abdofoda.workers.dev
